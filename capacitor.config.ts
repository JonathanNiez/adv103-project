import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ADV103Projec',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
